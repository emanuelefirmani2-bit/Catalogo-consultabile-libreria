import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Catalogo di consultazione: nessuna config particolare per ora.
  // TODO ESP32: in futuro, route handler /api/led per inviare comandi al gateway pick-to-light.
};

export default nextConfig;
