export default function StatistichePage() {
  return (
    <div className="rounded border border-[var(--color-border)] bg-[var(--color-surface)] p-8 text-center">
      <h2 className="font-display text-2xl">Dashboard statistiche</h2>
      <p className="mt-2 text-sm text-[var(--color-muted)]">
        In arrivo: totali, completezza per campo, distribuzione per anno e per
        casa editrice, record marcati per verifica.
      </p>
    </div>
  );
}
